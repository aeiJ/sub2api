# Beads Configuration File
# This file configures default behavior for bd commands in this repository.

issue-prefix: "sub2api"

export:
  auto: false
  path: issues.jsonl
  interval: 60s
  git-add: false

import:
  path: issues.jsonl

sync.remote: "git+https://github.com/Wei-Shaw/sub2api.git"